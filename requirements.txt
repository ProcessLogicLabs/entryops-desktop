# EntryOps Application Requirements
# Compatible with Python 3.8+

# Core dependencies (cross-platform)
PyQt5>=5.15.0
pandas>=1.3.0
openpyxl>=3.0.0
pdfplumber>=0.10.0
Pillow>=9.0.0
reportlab>=3.6.0
requests>=2.25.0
pytz>=2024.1
tzdata>=2024.1

# OCR fallback for scanned / image-only PDFs. pytesseract needs the
# Tesseract binary installed separately; when the binary is absent the
# NullBackend in Entryops/ocr/ keeps behavior unchanged.
pytesseract>=0.3.13
pymupdf>=1.27.0

# Legacy .xls support — used by the OCRMill PDF Processing tab when an
# .xls commercial invoice is dropped (added in v1.6.0). xlrd 2.0+
# deliberately dropped .xlsx support, so .xlsx routes through openpyxl
# instead (already a dependency above).
#
# (Playwright was previously bundled here for the ISF Filing tab; that
# tab was retired in v1.6.1 and Playwright is no longer needed by
# EntryOps. The standalone ISF agent will pull it in on its own.)
xlrd>=2.0.1

# Platform-specific (Windows only)
pywin32>=300; sys_platform == 'win32'

# Optional: For creating executable bundles
# PyInstaller>=6.0.0
